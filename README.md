# Attest: Verified Actions for AI Agents

A reliability layer for AI agents taking side-effecting actions. Attest records actions durably before execution, verifies resulting system state independently of tool responses, reconciles ambiguous outcomes, and prevents agents from claiming completion without verified evidence.
